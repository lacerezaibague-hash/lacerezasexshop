// AI features have been disabled by user request.
// This file is kept as a placeholder to prevent import errors if referenced elsewhere,
// but it no longer contains any logic or dependencies on @google/genai.

export const generateProductDescription = async (): Promise<string> => {
  return "";
};

export const suggestProductTitle = async (): Promise<string> => {
  return "";
};

export const generateProductImage = async (): Promise<string> => {
  return "";
};
